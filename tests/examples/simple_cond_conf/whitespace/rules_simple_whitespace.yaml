---
# Global settings
actions_source_modules:
  - "tests.examples.code.actions"

# Rule sets for simple conditions tests
rules:
  default_rule_set:
    whitespace:
      RULE_1:
        simple_condition: input.text=="super hero super hero"
        action: concatenate_str
        action_parameters:
          list_str:
            - OK
